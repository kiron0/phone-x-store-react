import React from 'react';
import './ShowAll.css'

const ShowAll = () => {
    return (
        <div>
            <button id="show-all" className="show-all d-none">Show All</button>
        </div>
    );
};

export default ShowAll;